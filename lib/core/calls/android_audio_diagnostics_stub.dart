// Pure-Dart stub selected when `dart.library.ui` is NOT available (daemon
// build via `dart compile exe`). No MethodChannel, no platform to query.
class AndroidAudioDiagnostics {
  AndroidAudioDiagnostics._();

  /// Always null outside a Flutter host — nothing to report.
  static Future<String?> summarize({bool probe = true}) async => null;
}
